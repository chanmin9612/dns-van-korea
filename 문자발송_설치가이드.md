# DNS 문자발송(솔라피) 설치 가이드

CS 페이지의 **📱 문자 발송** 버튼을 실제로 작동시키려면 아래 4단계만 하면 됩니다.
코드(프론트 버튼 연결 + 서버 함수)는 이미 넣어 두었습니다.

## 1. 솔라피 가입 & 발신번호 등록
- https://solapi.com 가입
- [발신번호 등록]에서 대표번호 1670-9891 또는 휴대폰을 등록
  (통신가입증명원 등 서류 필요 · 법적으로 사전등록 필수)

## 2. API 키 발급
- 솔라피 콘솔 → [개발/연동] → API Key 생성 → **API Key**와 **API Secret** 복사

## 3. Supabase에 시크릿 등록 (키를 코드에 넣지 않고 서버에만 보관)
프로젝트 폴더에서 터미널로:
```
supabase secrets set SOLAPI_API_KEY=발급받은_API_KEY
supabase secrets set SOLAPI_API_SECRET=발급받은_API_SECRET
supabase secrets set SOLAPI_SENDER=발신번호숫자만   (예: 16709891)
```

## 4. 함수 배포
```
supabase functions deploy send-sms
```
> Supabase CLI가 없으면: `npm i -g supabase` 후 `supabase login`, `supabase link --project-ref <프로젝트ref>` 한 번 실행.

## 완료 후
CS 관리 → 📤 안내 문구 → 문구 선택 → **📱 문자 발송** → 수신번호 확인 후 발송됩니다.
- 한글 45자(90바이트)가 넘으면 자동으로 LMS(장문)로 발송됩니다.
- 발송 실패 시 화면에 사유가 표시되고, 그때는 [복사]로 보내면 됩니다.

## 비용(대략)
- SMS 단문 약 20원 / LMS 장문 약 30~40원 (솔라피 표준 단가 기준, 변동 가능)

## 문제 해결
- "시크릿이 설정되지 않았습니다" → 3단계 시크릿 등록 확인
- "발신번호 미등록" 류 오류 → 솔라피에서 발신번호 등록/승인 상태 확인
